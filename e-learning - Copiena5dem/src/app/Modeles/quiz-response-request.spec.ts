import { QuizResponseRequest } from './quiz-response-request';

describe('QuizResponseRequest', () => {
  it('should create an instance', () => {
    expect(new QuizResponseRequest()).toBeTruthy();
  });
});
